"""AIStack — companion Python package for the AI Engineering handbook.

This package is intentionally minimal. The handbook content lives in `docs/`;
this package exists so `pip install -e .[docs]` works for building the site
and so future runnable examples have a place to live.
"""

__version__ = "0.1.0"
